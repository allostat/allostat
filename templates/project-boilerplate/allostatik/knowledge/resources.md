# [PROJECT-NAME] — Resources

External references for this project — docs, repos, and sites Claude pulls in when relevant. These are things you point at (local copies live in `docs/`). Each entry has a `name`, a `description` (written as when to reach for it), and a `location` (URL or place) — the same shape as a skill.

**Last updated:** [YYYY-MM-DD]

## References

```yaml
- name: allostatik
  description: the methodology this project's configuration is built on (the layer model, the templates, the loop); reach for it when revisiting how the setup is structured or extending it
  location: "https://github.com/allostatik/allostatik"

# Add an entry per reference, same three fields. Example:
# - name: Partner API docs
#   description: the partner's REST reference; pull when implementing or debugging their endpoints
#   location: https://example.com/docs
```
